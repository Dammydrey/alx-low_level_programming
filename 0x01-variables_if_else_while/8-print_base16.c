#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - print numbers in base 16
 *Return: Always 0 (success)
 */
int main(void)
{
	int num;
	char fe;

	for (num = 0; num <= 9; num++)
		putchar((num);

	for (fe = 'a'; fe <= 'f'; fe++)
		putchar(fe);
	return (0);
}
